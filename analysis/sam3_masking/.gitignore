# SAM3 masking: run artifacts stay local. The code + the noun table are what other
# servers pull; the videos/npz are regenerated by re-running the batch script.
outputs_actionnet/
outputs_actionnet_auto/
outputs_actionnet_door/
mask_ppt/
logs/
# Ad-hoc probe scratch (no script reads it; regenerate with probe_actionnet_nouns.py).
actionnet_task_nouns_probe.json
